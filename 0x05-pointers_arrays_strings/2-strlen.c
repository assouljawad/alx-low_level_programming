#include "main.h"
/**
* _strlen  - a poniter to int as argument an change its value.
* @s: poniter
* Return: return the lenght of string
*/
int  _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
