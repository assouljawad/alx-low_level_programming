#include "main.h"
/**
* mul - is functions that checks is the c is digit.
* @a: holdes the num value
* @b: holdes the num 2 value
* Return: if digit return 1 else return 0
*/

int mul(int a, int b)
{
	return (a * b);
}
