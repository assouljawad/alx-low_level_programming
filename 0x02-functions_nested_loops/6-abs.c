#include "main.h"
/**
 * _abs - Checks for the abs value of a number.
 * @n: The character to be checked.
 *
 * Return: 1 if character is lowercase, 0 otherwise.
 */
int _abs(int n)
{
	return (n < 0 ? -n : n);
}
