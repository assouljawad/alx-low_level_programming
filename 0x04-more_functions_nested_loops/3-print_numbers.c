#include "main.h"
/**
* print_numbers - is functions that checks is the c is digit.
* Return: if digit return 1 else return 0
*/

void print_numbers(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
